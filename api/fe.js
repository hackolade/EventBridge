const { generateModelScript } = require('../forward_engineering/generateModelScript');
const { validate } = require('../forward_engineering/validation/validate');

module.exports = {
	generateModelScript,
	validate,
};
